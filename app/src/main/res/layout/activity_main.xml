<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="fill_parent"
    android:layout_height="fill_parent" android:paddingLeft="0dp"
    android:paddingRight="0dp"
    android:paddingTop="0dp"
    android:paddingBottom="0dp" tools:context=".MainActivity"
    android:background="#ff000000" >

    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:id="@+id/defaultImageView"
        android:contentDescription="Your dream destination image is here..."
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:src="@drawable/placeholder" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="POI Name"
        android:textAlignment="center"
        android:gravity="center"
        android:id="@+id/POInameView"
        android:layout_marginBottom="40dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:textColor="#ffffff"
        android:background="#ff000000"
        android:textStyle="bold"
        android:alpha="0.9" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:text="POI Description"
        android:textAlignment="center"
        android:gravity="center"
        android:id="@+id/POIdescriptionView"
        android:layout_alignTop="@+id/POInameView"
        android:layout_alignLeft="@id/POInameView"
        android:layout_marginTop="30dp"
        android:layout_alignBottom="@id/defaultImageView"
        android:textColor="#ffffff"
        android:background="#ff000000"
        android:alpha="0.9" />

</RelativeLayout>
